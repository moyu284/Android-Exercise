<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity4.Main4Activity"></activity>
        <activity android:name=".Activity3.Main3Activity" />
        <activity android:name=".Activity2.Main2Activity" />
        <activity
            android:name=".StartActivity.StartActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity5.Main5Activity" />
        <activity android:name=".Activity1.MainActivity" />
        <receiver android:name=".Activity4.MyReceive">
            <intent-filter>
                <action android:name="broadcast"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>